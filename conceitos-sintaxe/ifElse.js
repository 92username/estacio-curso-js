// pseudo codigo
// Estrutura de decisão e operadores lógicos.
if (condição1 && condição2){ // E
    instrução1;
    instrução2;
    }
    if (condição1 || condição2){ // ou
        instrução1;
        instrução2;
        }

        if (!condição1){ // negação
            instrução1;
            instrução2;
            }

// Exemplo de uso de if e else / pseudo código
            if(número fornecido é inteiro e positivo){
                Guarde o número em uma variável;
           }else{
                Avise ao usuário que o número não é válido;
                Solicite ao usuário que insira novamente um número;
           }

           if (numero1 < 0){
            instrução1;
            }else if(numero == 0){
                 instrução2;
            }else{
                 instrução3;
            }
            
            